import { IUserProvider } from "../ICreateUserProvider";

export class CreateUserProvider implements IUserProvider {
    
    constructor() {

    }

    createUser(user: IUserProvider): Promise<void> {
        
    }

}